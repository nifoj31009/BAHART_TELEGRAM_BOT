"""
The verbose WS test client has been intentionally omitted in this branch per the repository owner's request.

If you need to re-enable the verbose debug client later, restore the original
app/ws_verbose.py contents from the commit that added the scaffold, or replace
this file with a copy of the verbose client. The original verbose client printed
connection details and all incoming events and was useful for debugging.

For now, this placeholder prevents automatic execution and keeps the repository
clean of the debug script.
"""

# Verbose client disabled. No runtime code here.
