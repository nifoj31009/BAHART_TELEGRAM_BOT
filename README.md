# Updated README note: verbose client omitted

The verbose debug client (app/ws_verbose.py) has been disabled in this branch
by request. To re-enable it, restore app/ws_verbose.py from the scaffold commit
or contact the repository maintainer.
